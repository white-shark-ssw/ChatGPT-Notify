# Notification Channel

这个文件本身不发送通知。

永久 Draft PR `#1` 会通过 `notification-channel` 分支对本文件保留一个小改动，使 PR 始终存在并充当 ChatGPT 通知事件入口。

真正的通知消息写在 PR `#1` 的顶层 Conversation 评论中。

## Channel state

`OPEN — KEEP THIS BRANCH AND PR`
